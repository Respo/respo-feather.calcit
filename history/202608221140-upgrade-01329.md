# Calcit 0.13.29 升级

- 将 Calcit、`@calcit/procs`、CI action 与快照校验命令统一到 0.13.29 / `calcit` / `caps`。
- 保持 canonical `calcit.cirru`，更新依赖与 Yarn lock，并把部署限制在 main push。
- 保留升级前的 quality baseline；新版 analyzer 初次报告 schemaDynamic/unresolved 为 23/30，经过将两个可确定的组件参数收窄为 `Map`/`String` 后恢复到 21/28，未放宽质量门禁。
- 本地通过 `caps --ci`、check-only、升级 baseline 和 JS codegen；Vite build 需 Node 20.19+ 或 Node 22/24，CI 已使用 Node 24。
