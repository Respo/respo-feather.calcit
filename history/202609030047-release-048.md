# Release 0.4.8 / 发布 0.4.8

- Release the Calcit 0.13.75 and fully aligned Respo dependency graph from PR #29.
- 发布 PR #29 中基于 Calcit 0.13.75 且完全对齐的 Respo 依赖图。
- Downstream applications can now resolve the chain with `caps --strict` without transitive version-selection warnings.
- 下游应用现在可以通过 `caps --strict` 解析依赖链，不再出现传递版本选择告警。
